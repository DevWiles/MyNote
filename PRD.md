# MyNote 产品需求文档（PRD）

> 版本：v0.1 ｜ 最后更新：2026-06-29 ｜ 状态：草案

## 1. 产品概述

MyNote 是一款**简洁清新**的跨平台桌面应用，把 **待办（To-do）**、**笔记（Note）** 与 **日程安排（Schedule）** 三者融为一体，并接入 **DeepSeek 大模型**，根据用户的任务与笔记自动生成**日报 / 周报 / 月报**。

- **目标用户**：需要在同一处管理任务、记录想法、安排日程的个人用户（独立开发者、交易者、知识工作者等）。
- **核心价值**：
  1. 一处管理「要做的事 + 记下的事 + 安排的事」，减少多 App 切换。
  2. AI 自动汇总，省去手写日报/周报/月报的负担。
  3. 数据本地优先，隐私可控，可选 WebDAV 多设备同步。
- **设计基调**：小清新、留白充足、低饱和配色、轻量无负担。

## 2. 目标与非目标

### 2.1 目标（v1.0）
- 完成 待办 / 笔记 / 日程 三大模块的基础闭环。
- 接入 DeepSeek，实现日报/周报/月报自动生成。
- 本地存储 + WebDAV 同步可用。
- Windows 与 macOS 双平台打包发布。

### 2.2 非目标（暂不做）
- 多人协作 / 团队空间。
- 移动端（iOS/Android）。
- 复杂的项目管理（甘特图、依赖关系等）。
- 自建账号体系与云服务器（同步交由 WebDAV）。

## 3. 技术栈建议

| 维度 | 选型 | 理由 |
|------|------|------|
| 框架 | **Tauri 2.x**（Rust + Web 前端） | 体积小、启动快、原生体验好，契合「轻量小清新」定位；天然跨 Win/Mac |
| 前端 | React + TypeScript + Vite | 生态成熟，组件化高效 |
| UI | Tailwind CSS + 自定义清新主题 | 易做留白、低饱和配色 |
| 本地存储 | SQLite（通过 Tauri 插件） | 结构化查询任务/日程，性能好 |
| 笔记内容 | Markdown 文件 + 元数据入库 | 纯文本可移植，便于 WebDAV 同步与备份 |
| 同步 | WebDAV 客户端 | 用户自带网盘（坚果云、NextCloud 等），无需自建服务 |
| AI | DeepSeek API | 生成日报/周报/月报 |

> 备选：若更熟悉前端可用 Electron，但体积与内存占用较大，不推荐用于本项目的「轻量」定位。

## 4. 功能需求

### 4.1 待办（To-do）
- 创建/编辑/完成/删除任务；支持子任务（可选）。
- 字段：标题、备注、截止日期、优先级（高/中/低）、标签、关联日程。
- 视图：今日、即将到期、全部、已完成。
- 快捷输入：顶部输入框一行速记，回车即建。
- 完成的任务进入归档，作为 AI 报告的数据源。

### 4.2 笔记（Note）
- Markdown 编辑器：**左右分屏**，左侧写源码、右侧实时预览。
- 字段：标题、正文、标签、创建/更新时间。
- 组织：标签 + 全文搜索；可选文件夹/笔记本。
- 双向关联：笔记可关联任务或某日日程（为报告提供上下文）。

### 4.3 日程（Schedule）
- 日历视图：月/周/日切换。
- 事件：标题、起止时间、全天事件、提醒、备注。
- 与待办联动：带截止日期的任务可在日历上展示。
- 桌面通知提醒（到点提醒）。

### 4.4 AI 报告（DeepSeek）
- **日报**：汇总当日完成任务、新增笔记、日程，生成总结与亮点。
- **周报 / 月报**：按时间范围聚合，给出进展、趋势、待改进。
- 触发方式：**仅手动触发**（用户一键生成），不做定时自动生成。
- 交互：一键生成 → 预览 → 可编辑 → 保存为笔记 / 导出 Markdown。
- 配置：用户在设置中填入 DeepSeek API Key，本地保存；可自定义报告 Prompt 模板。
- 隐私：仅在用户主动生成时调用 API，明确告知数据将发送至 DeepSeek。

### 4.5 存储与同步
- **本地优先**：所有数据先落本地（SQLite + Markdown 文件）。
- **WebDAV 同步**：配置服务器地址、账号、密码；支持手动同步与定时同步。
- 冲突处理：以「最后修改时间」为基础，冲突时保留两份并提示用户（v1 简化策略）。

### 4.6 设置
- 主题（浅色/深色，清新主色可选）。
- DeepSeek API 配置与报告模板。
- WebDAV 配置与同步频率。
- 数据导入/导出（备份）。

## 5. 非功能需求
- **性能**：冷启动 < 2s，常用操作无卡顿。
- **体积**：安装包尽量小（Tauri 优势）。
- **隐私**：默认全本地；任何外发（AI、WebDAV）均需用户显式配置与授权。
- **可靠性**：本地数据有自动备份，避免同步导致数据丢失。
- **跨平台**：Windows 10+、macOS 12+ 行为一致。

## 6. 数据模型（初稿）

```
Task        { id, title, note, due_at, priority, status, tags[], schedule_id?, created_at, updated_at }
Note        { id, title, body_md, tags[], task_id?, schedule_id?, created_at, updated_at }
Schedule    { id, title, start_at, end_at, all_day, remind_at, note, created_at, updated_at }
Report      { id, type(daily|weekly|monthly), range_start, range_end, content_md, created_at }
Settings    { theme, deepseek_api_key, report_template, webdav_config, sync_interval }
```

## 7. 里程碑（建议）

| 阶段 | 内容 | 产出 |
|------|------|------|
| M0 | 项目脚手架：Tauri + React + Tailwind，主题与基础布局 | 可运行空壳 |
| M1 | 待办模块闭环 | 任务增删改查 + 视图 |
| M2 | 笔记模块 + Markdown 编辑器 | 笔记闭环 |
| M3 | 日程模块 + 日历视图 + 提醒 | 日程闭环 |
| M4 | DeepSeek 接入：日/周/月报 | AI 报告可用 |
| M5 | WebDAV 同步 + 设置页 | 多设备同步 |
| M6 | 打磨 UI、跨平台打包发布 | Win/Mac 安装包 |

## 8. 已确认决策（Decisions）
1. ✅ 笔记编辑器：**左右分屏**，左侧 Markdown 源码、右侧实时预览。
2. ✅ AI 报告：**仅手动触发**，不做定时自动生成。

## 9. 待确认问题（Open Questions）
1. WebDAV 冲突策略 v1 是否可接受「保留双份」的简化方案？
2. 是否需要全局快捷键（如随处呼出速记窗口）？
